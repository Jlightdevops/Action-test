name: shell commands
push

on:
job: 
  run-shell-commands:
   run-on: ubuntu-latest
   steps:
     -name: echo string
      run:  echo "hello"

     -name: multi line string
      run: |
       pwd
       ls
       node -v
       ls -a